<%= form_for(@confession) do |f| %>
  <% if @confession.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@confession.errors.count, "error") %> prohibited this confession from being saved:</h2>

      <ul>
      <% @confession.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :confession_text %><br>
    <%= f.text_field :confession_text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
