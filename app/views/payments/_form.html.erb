<%= form_for(@payment, remote: true) do |f| %>
  <% if @payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@payment.errors.count, "error") %> prohibited this payment from being saved:</h2>

      <ul>
      <% @payment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div id="description">
  	<p>We will be adding a convenient opportunity to donate to your favorite foodbank soon!  Share your e-mail with us to be notified when you can give!"</p>
  </div>

  <div class="field">
    <%= f.label :email %><br>
    <%= f.text_field :email, :id => "payment_submit" %>
  </div>
  <div class="actions">
    <%= f.submit "Sign-Up!" %>
  </div>
<% end %>
