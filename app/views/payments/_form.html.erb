<%= form_for(@payment, remote: true) do |f| %>
  <% if @payment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@payment.errors.count, "error") %> prohibited this e-mail from being saved:</h2>

      <ul>
      <% @payment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  

<div id="payment-text">
  <div id="confess_field" class="field">
    <%= f.text_field :email, 
    	:id => "payment_text_field",
    	:value => "", 
	    :placeholder => "Please type your email address here...",
	    :style => "text-align: Center"
	     					 %>
  </div>
  <br>
  <div class="actions", id="submit_confession">
    <%= f.submit "Sign-Up!", :id => "confess_submit" %>
  </div>
  </div>
<% end %>
